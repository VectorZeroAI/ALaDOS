[project]
name = "alados"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [ ] # ADD THE DEPS HERE. json-repair, sentense-transformers, psycopg

[project.scripts]
alados_start = "python.main:main"
